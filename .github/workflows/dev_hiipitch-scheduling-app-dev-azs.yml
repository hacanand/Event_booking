name: Build and Deploy Next.js App

on:
  push:
    branches:
      - dev
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code
      - name: Checkout Code
        uses: actions/checkout@v4

      # Step 2: Set up Node.js environment
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'
          cache: 'npm'

      # Step 3: Install dependencies and build the application
      - name: Install Dependencies and Build
        env:
          MONGO_URI: ${{ secrets.MONGO_URI }}
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.CLERK_PUBLISHABLE_KEY }}
        run: |
          npm ci
          npm run build

      # Step 4: Prune dev dependencies to reduce package size
      - name: Prune Node Modules
        run: |
          npm prune --production
          echo "Pruned non-production dependencies."

      # Step 5: Create the deployment package
      - name: Create Deployment Archive
        run: |
          mkdir -p deployment-package
          cp -r .next package.json node_modules public deployment-package/
          echo "Ensuring required files are included in the deployment package:"
          ls -lh deployment-package
          rm -rf deployment-package/.next/cache
          echo "Removed unnecessary cache files from the deployment package."
          zip -r deployment-package.zip deployment-package

      # Step 6: Verify the deployment package
      - name: Verify Deployment Package
        run: |
          echo "Verifying the deployment package size and contents:"
          du -sh deployment-package.zip
          unzip -l deployment-package.zip

      # Step 7: Deploy to Azure Web App
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'hiipitch-scheduling-app-dev-azs'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_4385ACEF9DF8458AA19B97ACE948ED13 }}
          package: deployment-package.zip
